config:
  target: "http://localhost:3004"
  phases:
    - duration: 20
      arrivalRate: 1000
  processor: "./artilleryFunc.js"

scenarios:
  - name: "Get ID"
    flow:
      - function: "generateRandomData"
      - get:
          url: "/?id={{ id }}"